# ── Network simulation & analysis ─────────────────────────────────────────────
numpy
pandas
matplotlib
seaborn
pyyaml

# ── HAR crawler ───────────────────────────────────────────────────────────────
selenium>=4.0
selenium-wire==5.1.0
blinker==1.4          # 1.5+ removes _saferef used by selenium-wire internals
setuptools<81         # 81+ removes pkg_resources used by selenium-wire's bundled mitmproxy
pyOpenSSL<23.0        # 23.0+ removes X509.get_extension() used by selenium-wire's bundled mitmproxy
selenium-stealth
webdriver-manager
tqdm

# ── AWS (lambda / S3) ─────────────────────────────────────────────────────────
boto3
botocore
